This folder is reserved for any local files you want to keep with the project
(for example, a favicon, or a small local fallback image).

Photos for the presentation itself are not expected to live here — they are
loaded from public Google Drive links, set in `js/data.js`. See the main
README.md for instructions.
